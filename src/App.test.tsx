import { describe, it, expect } from 'vitest'

describe('App Component', () => {
  it('should pass basic test', () => {
    expect(true).toBe(true)
  })
  
  it('can perform simple arithmetic', () => {
    expect(1 + 1).toBe(2)
  })
})
